import React from 'react';
import { cellObj as CellObj } from "../../utils/generateMap";

const Cell = ({cell}: {cell: CellObj}) => {
    return (
        <div className='cell'>
            {/* {cellObj} */}
        </div>
    );
}

export default Cell;